import { StyleSheet } from 'react-native';

const stylesNotificationScreen = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});

export default stylesNotificationScreen; 