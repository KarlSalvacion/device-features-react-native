import { StyleSheet } from "react-native";

const stylesHeartAnimation = StyleSheet.create({
    heartContainer: {
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: [{ translateX: -40 }],
    },
});

export default stylesHeartAnimation;